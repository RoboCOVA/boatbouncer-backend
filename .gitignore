node_modules/
.env

# Firebase Admin SDK (use GOOGLE_APPLICATION_CREDENTIALS or FIREBASE_SERVICE_ACCOUNT_JSON)
src/config/firebase/firebase-adminsdk.json

# logs
logs/
*.log
npm-debug.log*

.adminjs/
logs/

dist/
build/

# env backups (.env is ignored, but variants like .env.port3000.bak were not —
# they hold the same live credentials)
.env.*
!.env.example

# multer's upload staging; files here are transient
src/config/temp/*
!src/config/temp/.gitkeep
